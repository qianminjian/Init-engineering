#!/usr/bin/env bash
# {{ project_name }} — bash 测试
set -euo pipefail

output="$(bash hello.sh)"
if [[ "$output" != *"Hello from {{ project_name }}"* ]]; then
  echo "FAIL: 期望包含 'Hello from {{ project_name }}'，实际: $output"
  exit 1
fi
echo "✓ hello.sh 通过"