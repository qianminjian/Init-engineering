# {{ server_name }}

> {{ server_description }}

MCP Server — {{ transport }} transport.

## Quick Start

```bash
{%- if language == 'typescript' %}
npm install
npm run build
{%- elif language == 'python' %}
pip install -e .
{%- endif %}
```

## Claude Code Config

```json
{
  "mcpServers": {
    "{{ server_name }}": {
      "command": "{% if language == 'typescript' %}node{% elif language == 'python' %}python{% endif %}",
      "args": ["{% if language == 'typescript' %}dist/index.js{% elif language == 'python' %}-m {{ server_name }}{% endif %}"{% if transport == 'sse' %},
      "url": "http://localhost:3000/sse"{% elif transport == 'streamable-http' %},
      "url": "http://localhost:3000/mcp"{% endif %}
      ]
    }
  }
}
```

## Tools

<!-- Generated by ae init — list your MCP tools here -->

## Development

{% if language == 'typescript' %}
- Build: `npm run build`
- Dev: `npm run dev`
{%- elif language == 'python' %}
- Run: `python -m {{ server_name }}`
- Test: `pytest`
{%- endif %}

## Architecture

```
{{ server_name }}/
├── src/           # Tool implementations
├── tests/         # Test files
└── package.json   # Dependencies & scripts
```
