[project]
name = "{{ repo_name }}-shared"
version = "0.1.0"
description = "{{ repo_description }} — shared utilities package"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"