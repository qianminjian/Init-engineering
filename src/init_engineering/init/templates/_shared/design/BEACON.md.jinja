> 来源：@design/INDEX.md | 创建：{{ current_year }}-{{ '%02d' | format(1) }}-01 | 更新：{{ current_year }}-{{ '%02d' | format(1) }}-01 | 阶段：初始化

## 目标与成功标准

1. **{{ project_name }}** — {{ project_description or '（待补充项目描述）' }}
2. （根据实际项目填写具体目标与可验证的成功标准）

## 范围边界

**做：**
- {{ project_name }} 核心功能开发{% if language == 'typescript' %} — TypeScript + {{ test_runner }} 测试{% elif language == 'python' %} — Python + {{ test_runner }} 测试{% endif %}

**不做：**
-

## 设计决策

| # | 决策 | 理由 | 日期 |
|---|------|------|------|
| 1 | 项目类型: {{ project_type }} | ae init 初始化 | {{ current_year }}-{{ '%02d' | format(1) }}-01 |
| 2 | 语言: {{ language }}{% if use_typescript %} (TypeScript){% endif %} | ae init 默认 | {{ current_year }}-{{ '%02d' | format(1) }}-01 |
{% if package_manager %}| 3 | 包管理器: {{ package_manager }} | ae init 默认 | {{ current_year }}-{{ '%02d' | format(1) }}-01 |{% endif %}

## 当前状态

**阶段：** 初始化

**下一步：** 填写目标与成功标准，明确项目范围边界

**阻塞项：** 无

## 设计演进日志

| 日期 | 变更 | 原因 |
|------|------|------|
| {{ current_year }}-{{ '%02d' | format(1) }}-01 | 项目初始化 | ae init 脚手架生成 |

## 待解决问题

[Q?] 项目目标与成功标准待确认
[Q?] 技术选型细节待评审

## 引用文件

@design/INDEX.md — 文档索引{% if language == 'python' %}@.claude/rules/pytest-memory-management.md — pytest 16G 内存约束{% endif %}
