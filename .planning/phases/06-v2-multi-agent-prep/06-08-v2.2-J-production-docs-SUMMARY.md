---
phase: 06
plan: 06-v2.2-J-production-docs
subsystem: production-documentation
tags: [docs, production, troubleshooting, api-reference, e2e, beacon-decision-19, final]
dependency_graph:
  requires:
    - phase-06-v2.2-G-checkpoint-state-typing
    - phase-06-v2.2-H-round-result-gate
    - phase-06-v2.2-I-init-module-split
  provides:
    - 4 production docs (permanent assets, not gitignored)
    - BEACON decision 19 (v2.2 闭环记录)
  affects:
    - docs/production-deployment.md (new)
    - docs/troubleshooting.md (new)
    - docs/api-reference.md (new)
    - docs/e2e-real-run.md (new)
    - design/BEACON.md (decision 19 added)
tech-stack:
  added: []
  patterns:
    - permanent assets in docs/ (no gitignore)
    - BEACON decision table 倒序累积
    - 反向引用禁令遵守（核心代码不引用 _proc-use/）
key-files:
  created:
    - docs/production-deployment.md (80 lines)
    - docs/troubleshooting.md (77 lines)
    - docs/api-reference.md (80 lines)
    - docs/e2e-real-run.md (47 lines)
  modified:
    - design/BEACON.md (decision 19 + header date + current status)
decisions:
  - 4 docs 永久资产放 docs/ 而非 design/（user-facing 生产文档 vs design 内部设计文档）
  - BEACON 决策 19 记录 v2.2 闭环（Wave 3 P2 改进 + atdo 防护规则化）
  - 文档行数严格控制 ≤80 / ≤60（BEACON 规范的纪律延伸）
  - 故障排查 5 场景映射 BEACON 决策 11/12/18/19 + G.3
metrics:
  duration_min: ~15
  tasks_done: 6
  files_modified: 1
  files_created: 4
---

# Phase 06 Plan v2.2-J: v2.2 闭环 + 生产文档 Summary

**One-liner:** v2.2 FINAL phase — 4 件生产文档（permanent assets）+ BEACON 决策 19，
记录 v2.2 闭环完成 + atdo 防护规则化，为用户 manual gate 决策做准备。

---

## What Was Built

### 1. `docs/production-deployment.md` (80 行)

环境要求 + 安装 + CLI 速查 + 监控 + 升级路径 + 已知限制：

- Python 3.12+ 必需（PEP 695 内联 Generic[T]）
- `ANTHROPIC_API_KEY` 环境变量必需（v2.0 路径）
- pip / uv 两种安装方式
- 无 state migration（v1.1 / v2.0 schema 向后兼容）

### 2. `docs/troubleshooting.md` (77 行)

5 个常见故障场景：

1. `PydanticSerializationError` → 引用 BEACON 决策 G.3
2. `P0.1 Agent Tools 未连接` → 引用决策 12
3. 96GB 内存爆炸事故 → 引用 `CLAUDE.md` §⚠️ + `agent-spawn-timeout.md`（决策 19）
4. TDD 标签错位 → 引用决策 18 + `atdo-runtime-smoke-policy.md`
5. init module 大文件 → Phase I 拆分后入口（`InitWorker` / `AnswersMap` / `config.AnswersLoader` / `scaffold.TemplateEngine`）

### 3. `docs/api-reference.md` (80 行)

CLI v1/v2 命令完整参考：

- `ae init` 14 个 option
- `ae dev-loop` 13 个 option（含 `--use-v1` / `--use-v2` 互斥规则）
- `ae status` 输出规范
- `ae checkpoint` v1.1 + v2.0 子命令
- `ANTHROPIC_API_KEY` 环境变量
- 7 类退出码（0-6）

### 4. `docs/e2e-real-run.md` (47 行)

端到端真跑指南：

- 前置：`ANTHROPIC_API_KEY` + `ae --version` 验证
- 单 Agent：`ae dev-loop "..." --use-v2 --max-rounds 1`
- 多 Agent：`ae dev-loop "..." --use-v2 --max-rounds 5`
- 故障排查 cross-reference 到 troubleshooting.md

### 5. `design/BEACON.md` 更新

- 决策 19 追加：`v2.2 闭环完成 + 生产就绪`（Wave 3 P2 改进 + atdo 防护规则化）
- header 更新：阶段 `v2.2 闭环完成`
- 当前状态更新：Phase G/H/I/J 全部 commit hash 记录
- 设计演进日志：追加 2026-06-26 Phase J + Phase G-I 条目

---

## TDD Gate Compliance

Phase J 为文档 phase，无 TDD gate（无 source code 变更）。

---

## Verification Results

| 检查项 | 期望 | 实际 | 状态 |
|--------|------|------|------|
| 4 docs 永久资产存在 | 4 | 4 | PASS |
| docs 行数 ≤80 / ≤60 | ≤80 / ≤60 | 80 / 77 / 80 / 47 | PASS |
| BEACON.md 决策 19 存在 | 是 | 是（`\| 19 \|`） | PASS |
| grep `ae dev-loop --use-v2` 命中 | ≥1 | 2 (production-deployment + troubleshooting) | PASS |
| `ae --help` exit code | 0 | 0 | PASS |
| ruff check auto_engineering/ | 0 errors | All checks passed! | PASS |

---

## Deviations from Plan

### Auto-fixed Issues

None — plan executed as written.

### Out-of-scope (deferred to follow-up)

| 项 | 文件 | 状态 |
|------|------|------|
| F401 unused `Verdict` import | `tests/test_loop_orchestrator.py:500` | pre-existing (Phase H 568baf1, 2026-06-26 00:24) — out of Phase J scope |

**注**：测试文件 lint warning 来自 Phase H commit，不属于 Phase J 改动范围（CLAUDE.md
scope boundary 规则：只 fix 当前 task 直接造成的 issue）。建议下一 phase（v3.0 或
Phase K）清理。

---

## Self-Check: PASSED

| 验证项 | 结果 |
|--------|------|
| `docs/production-deployment.md` 存在 | FOUND |
| `docs/troubleshooting.md` 存在 | FOUND |
| `docs/api-reference.md` 存在 | FOUND |
| `docs/e2e-real-run.md` 存在 | FOUND |
| BEACON 决策 19 存在 | FOUND |
| Commit `6b73ced` 存在 | FOUND |
| `docs/` 不在 `.gitignore` | PASS（grep ^docs/ gitignore = 0） |

---

## Final Status

Phase J 完成。v2.2 全部闭环（Phase G/H/I + J）。等待用户 manual gate 决策 v2.2
完成判断 + v3.0 启动决策（orchestrator 会发起 AskUserQuestion）。

## 引用

- `design/BEACON.md` 决策 11/12/19 + 演进日志
- `docs/production-deployment.md` · `docs/troubleshooting.md`
- `docs/api-reference.md` · `docs/e2e-real-run.md`
- `docs/atdo-runtime-smoke-policy.md`（既有）
