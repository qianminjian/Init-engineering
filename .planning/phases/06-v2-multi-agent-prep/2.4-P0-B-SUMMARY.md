---
phase: 06-v2-multi-agent-prep
plan: 2.4-P0-B
subsystem: CLI / Agent Runtime
tags: [refactor, type:auto, tdd]
requires: []
provides: ["real-agent-runtime"]
affects: ["cli._build_v2_agent_runtime", "Orchestrator agent dispatch"]
tech-stack:
  added: []
  patterns: ["factory-function-registration", "lambda-registration"]
key-files:
  created:
    - tests/test_cli_v2_agent_runtime_real.py
  modified:
    - auto_engineering/cli.py
decisions:
  - "P0-B: _build_v2_agent_runtime 使用真实 Agent(BaseAgent) 实例替代 _MockRoleAgent/_DeveloperAgentAdapter"
  - "reviewer role 不再注册 (v2.0 仅 architect/developer/critic)"
metrics:
  duration: "~4min"
  completed_date: "2026-06-27"
---

# Phase 2.4 Plan P0-B: CLI _build_v2_agent_runtime 真实 Agent 注册

**One-liner:** Replace _MockRoleAgent / _DeveloperAgentAdapter with real Agent(BaseAgent) instances for all 3 roles (architect, developer, critic).

## Task Execution

### Task 1: RED Test

Wrote 6 tests verifying that `_build_v2_agent_runtime` returns `AgentRuntime` with all 3 roles as real `Agent` (BaseAgent) instances. Tests cover:
- architect/developer/critic instanceof Agent check
- `llm` attribute presence (not present on mock adapters)  
- Type name does not contain "Mock" or "Adapter"
- All 3 roles registered
- reviewer NOT registered

**Commit:** `5e8074e` — `test(P0-B): add failing tests for _build_v2_agent_runtime real Agent registration`

### Task 2: GREEN Implementation

Replaced the `_build_v2_agent_runtime` function body:
- Removed `_DeveloperAgentAdapter` (68 lines of adapter wrapping DeveloperAgent)
- Removed `_MockRoleAgent` (26 lines of mock agent returning SimpleNamespace)
- Replaced with 3 `Agent(llm=llm, role=..., system_prompt=..., tools=tools)` registrations via `runtime.register`
- Removed `reviewer` registration (v2.0 uses only 3 roles)
- Removed unused imports: `SimpleNamespace`, `DeveloperAgent`, `RuntimeTask` from agents.developer
- Added imports: `Agent` from base, `ARCHITECT_SYSTEM_PROMPT`/`CRITIC_SYSTEM_PROMPT`/`DEVELOPER_SYSTEM_PROMPT` from prompts

**Commit:** `0109fee` — `feat(P0-B): replace _MockRoleAgent/_DeveloperAgentAdapter with real Agent instances`

### Task 3: REFACTOR

Not needed — code is clean as implemented.

### Task 4: Delete Adapter Classes

Done as part of GREEN implementation — `_MockRoleAgent` and `_DeveloperAgentAdapter` are fully removed from `auto_engineering/cli.py`.

### Task 5: Full Test Suite

693 passed, 1 skipped, 0 failures. Zero regressions.

## Verification

| Check | Result |
|-------|--------|
| 6 new tests pass | PASS |
| Full suite (693 tests) | PASS |
| Stale references to _MockRoleAgent | 0 found |
| Stale references to _DeveloperAgentAdapter | 0 found |
| Module imports compile | PASS |
| Git working tree clean | PASS |

## Deviations from Plan

None — plan executed exactly as written (TDD: RED → GREEN).

## TDD Gate Compliance

| Gate | Commit | Status |
|------|--------|--------|
| RED | `5e8074e` test(P0-B) | PASS |
| GREEN | `0109fee` feat(P0-B) | PASS |
| REFACTOR | N/A | Not needed |

## Known Stubs

None.

## Self-Check: PASSED

- [x] `tests/test_cli_v2_agent_runtime_real.py` exists
- [x] `auto_engineering/cli.py` modified with real Agent registrations
- [x] Commits `5e8074e` and `0109fee` exist in git log
- [x] `git log --oneline -3` shows both commits
