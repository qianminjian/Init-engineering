{
  "planName": "Auto-Engineering v2.5 — 生产就绪最终修复（11 项优化）",
  "planDescription": "v2.5: 基于 /tttt 深度架构审计 + 参考项目对比 (LangGraph Pregel/CrewAI)。v1.0 已移除, v2.0 纯路径。修复 3 P0(阻断) + 5 P1(重要) + 3 P2(建议)。",
  "phases": [
    {
      "id": "01",
      "name": "P0-A: ContractGate 真实实现 (不再 placeholder)",
      "goal": "ContractGate 真正检查 agent 间契约, 不是永远 pass。单 Agent 跳过, 多 Agent 时检查 .ae-contracts/ 下文件存在 + 格式正确。",
      "success_criteria": [
        "ContractGate 单 Agent 时仍 skip (passed=True)",
        "多 Agent 时检查 contracts_dir 下契约文件存在 (至少 1 个 .yml 或 .json)",
        "契约文件格式不正确 → Verdict.failed",
        "无契约文件 → Verdict.failed (不是 skip)",
        "test_gates.py ContractGate 新增 3 用例",
        "全量测试 PASS"
      ],
      "tasks": [
        "gates/contract.py: 从占位实现升级为真实检查",
        "读 .ae-contracts/ 目录, 检查至少 1 个 .yml/.json 文件存在",
        "YAML/JSON 格式校验 (至少能 parse, 不要求语义校验)",
        "test_gates.py 新增 3 用例: 单 Agent skip / 多 Agent 有契约 PASS / 多 Agent 无契约 FAIL",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "02",
      "name": "P0-B: CLI _build_v2_agent_runtime 不使用 Mock agent",
      "goal": "architect/critic 不再使用 _MockRoleAgent, 全部 3 个 role 使用真实 Agent(role, system_prompt, llm)。",
      "success_criteria": [
        "cli.py _build_v2_agent_runtime: architect/critic 注册为真实 Agent, 不是 _MockRoleAgent",
        "_MockRoleAgent 类删除",
        "_DeveloperAgentAdapter 简化或删除 (Agent 已统一)",
        "全量测试 PASS",
        "grep '_MockRoleAgent' 在 cli.py 中返回 0 处"
      ],
      "tasks": [
        "cli.py _build_v2_agent_runtime: architect 改用 Agent(llm, role='architect', system_prompt=ARCHITECT_SYSTEM_PROMPT)",
        "critic 改用 Agent(llm, role='critic', system_prompt=CRITIC_SYSTEM_PROMPT)",
        "删除 _MockRoleAgent + _DeveloperAgentAdapter 类",
        "更新 gates/safety.py + lint.py 引用 (如有)",
        "BEACON 决策 31: production agent routing",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "03",
      "name": "P0-C: ae dev-loop 端到端真实 LLM 验证",
      "goal": "在有 ANTHROPIC_API_KEY 的真实环境中跑 ae dev-loop, 确认 multi-round loop + gate check + convergence 全路径可运行",
      "success_criteria": [
        "ae dev-loop 'implement a hello world function' 在真 Claude API 上完成 ≥ 2 rounds",
        "每轮 Gate check (Safety/Lint) 通过",
        "ConvergenceJudge 触发至少 1 级停止判定 (硬上限或语义)",
        "输出 checkpoint_id 可被 ae checkpoint show 查看",
        "无 unhandled exception"
      ],
      "tasks": [
        "确保 ANTHROPIC_API_KEY 已设置",
        "运行 ae dev-loop, 观察 round loop + gate + convergence",
        "记录任何异常或 crash",
        "修复发现的 regression",
        "跑全量测试确认不破坏"
      ],
      "depends_on": ["01", "02"],
      "is_gate": true,
      "gateType": "manual"
    },
    {
      "id": "04",
      "name": "P1-A: loop/state.py 702 行拆 3 文件",
      "goal": "state.py → state/channels.py + state/checkpoint_envelope.py + state/metrics.py。旧 import 路径 re-export 保持向后兼容。",
      "success_criteria": [
        "state/channels.py: Channel ABC + LastValueChannel + AccumulatingChannel + BarrierChannel (≤ 300 行)",
        "state/checkpoint_envelope.py: CheckpointEnvelope Pydantic 模型 + 序列化三件套 (≤ 200 行)",
        "state/metrics.py: MetricsSnapshot + Signal (≤ 100 行)",
        "state/__init__.py re-export 所有符号, 旧 import 路径不破坏",
        "全量测试 685 PASS, 0 regression"
      ],
      "tasks": [
        "新建 state/channels.py",
        "新建 state/checkpoint_envelope.py",
        "新建 state/metrics.py",
        "state/__init__.py 改为 re-export",
        "删除或转发原 state.py",
        "跑全量测试验证向后兼容"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "05",
      "name": "P1-B: cli.py 1060 行拆为 cli/ + 子模块",
      "goal": "cli.py → cli/__init__.py(命令注册) + cli/dev_loop.py(dev_loop 命令) + cli/helpers.py(CancellationToken/TokenTracker/ErrorCategory)。遵循 Python 300 行限制。",
      "success_criteria": [
        "cli/__init__.py 含 @click.group() + @main.command() 注册 (≤ 200 行)",
        "cli/dev_loop.py 含 _run_v2_orchestrator + _build_v2_agent_runtime (≤ 350 行)",
        "cli/helpers.py 含 ErrorCategory/CancellationToken/TokenTracker/ProgressLogger (≤ 300 行)",
        "from auto_engineering.cli import main 不破坏",
        "全量测试 PASS",
        "每文件 ≤ 400 行 (Python limit)"
      ],
      "tasks": [
        "新建 cli/__init__.py (命令注册, re-export)",
        "抽取 cli/dev_loop.py (_run_v2_orchestrator, _build_v2_agent_runtime, OrchestratorRunResult)",
        "抽取 cli/helpers.py (ErrorCategory, CancellationToken, TokenTracker, ProgressLogger, _install_sigint_handler, classify_error)",
        "更新 from auto_engineering.cli import main (通过 __init__.py re-export)",
        "更新 test 文件中的 import (如有)",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "06",
      "name": "P1-C: ReadFileTool 加 project_root 沙箱",
      "goal": "ReadFileTool 接受 project_root 参数, 拒绝读取 project_root 外的文件。与 WriteFileTool/EditFileTool 沙箱一致。",
      "success_criteria": [
        "ReadFileTool.__init__ 接受 project_root: Path | None",
        "project_root 非 None 时, 读取路径必须 resolve 在 project_root 下, 否则抛 AEError",
        "project_root=None 时 (向后兼容), 不限制读取范围",
        "test_tools_integration.py 新增 workspace read 限制测试",
        "全量测试 PASS, 无 regression"
      ],
      "tasks": [
        "tools/file_tools.py: ReadFileTool 加 project_root 参数 + workspace 校验",
        "cli.py _build_v2_agent_runtime: ReadFileTool() 传入 project_root",
        "test_tools_integration.py 新增 2 用例: 沙箱内读 PASS / 沙箱外读 BLOCK",
        "跑全量测试"
      ],
      "depends_on": ["02"],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "07",
      "name": "P1-D: 删除 --max-cost / --multi 未实现 flag",
      "goal": "删除 CLI 中标注'未来实现'但从未实现的 flag。包括 --max-cost / --multi。实现的功能才暴露。",
      "success_criteria": [
        "cli.py dev_loop 命令无 --max-cost flag",
        "cli.py dev_loop 命令无 --multi flag",
        "相关 help text 删除",
        "全量测试 PASS"
      ],
      "tasks": [
        "cli.py: 删除 --max-cost option",
        "cli.py: 删除 --multi option",
        "删除 dev_loop 函数签名中对应参数",
        "删除 dev_loop 函数体中 '多 Agent 并行模式尚未实现' 代码段",
        "跑全量测试"
      ],
      "depends_on": ["05"],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "08",
      "name": "P1-E: loop/checkpoint.py 704 行拆 checkpoint/ 子模块",
      "goal": "checkpoint.py → checkpoint/store.py + checkpoint/envelope.py。与 state.py 拆分风格一致。≤ 400 行每个。",
      "success_criteria": [
        "checkpoint/store.py: SQLiteCheckpointStore + CheckpointMeta + SCHEMA_VERSION (≤ 400 行)",
        "checkpoint/envelope.py: Checkpoint + CheckpointError + CheckpointNotFoundError + CheckpointSchemaMismatchError (≤ 200 行)",
        "checkpoint/__init__.py re-export 所有符号",
        "loop/checkpoint.py → 转为 re-export (保持 import 兼容)",
        "全量测试 PASS"
      ],
      "tasks": [
        "新建 loop/checkpoint/store.py (SQLiteCheckpointStore 相关)",
        "新建 loop/checkpoint/envelope.py (Checkpoint 模型 + Error 类)",
        "loop/checkpoint.py → re-export only",
        "更新 import 引用",
        "跑全量测试"
      ],
      "depends_on": ["04"],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "09",
      "name": "P2-A: 清理过期注释 (Phase 1/2/3 引用)",
      "goal": "代码注释中所有 'Phase 1'/'Phase 2'/'Phase 3'/'v1.0'/'v1.1' 引用替换为当前状态描述或删除。",
      "success_criteria": [
        "grep -E 'Phase [123]|v1\\\\.0|v1\\\\.1' 在 production code 中为 0 处 (不含 docs/ + design/)",
        "保留的注释描述当前行为, 不描述历史演进",
        "全量测试 PASS"
      ],
      "tasks": [
        "grep 找到所有 'Phase ' 引用点",
        "按需更新或删除: 描述'当前实现了什么'而非'Phase X 应该实现什么'",
        "保留 design/BEACON.md 中的历史引用 (那是设计文档)",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "10",
      "name": "P2-B: 生产文档 — ae dev-loop quickstart + 故障排查",
      "goal": "docs/ 下新增 quickstart.md (1 页) + troubleshooting.md (常见故障)。新用户可以 5 分钟内跑通 ae dev-loop。",
      "success_criteria": [
        "docs/quickstart.md: 含安装/配置 ANTHROPIC_API_KEY/第一个 dev-loop 示例",
        "docs/troubleshooting.md: 含 CHECKPOINT_LOAD_FAILED/CONFIG_MISSING_API_KEY/BUDGET_EXCEEDED 等 5+ 常见错误 + 解决方案",
        "两个文档 ≤ 200 行各 (不超过设计文档长度)"
      ],
      "tasks": [
        "创建 docs/quickstart.md",
        "创建 docs/troubleshooting.md",
        "BEACON 决策 32: 生产文档基线"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "11",
      "name": "P2-C: 代码量审计 — 逐个文件过行数约束",
      "goal": "每文件 ≤ 400 行 (Python static 约束, 见 engineering-practices.md)。超出者均有拆分计划 (P1-A, P1-B, P1-E) 并在本 phase 验证。",
      "success_criteria": [
        "所有 auto_engineering/ 下 .py 文件 ≤ 400 行 (PY 约束, 见 .claude/rules/engineering-practices.md §3.1)",
        "或: 超出文件有明确 BEACON 决策说明'为什么暂时不拆'",
        "全量测试 PASS"
      ],
      "tasks": [
        "运行 find auto_engineering -name '*.py' -exec wc -l {} + | sort -rn",
        "对 > 400 行的文件: 确认 P1-A/P1-B/P1-E 已覆盖, 或新增 BEACON 决策",
        "验证所有拆分后的文件 ≤ 400 行",
        "跑全量测试"
      ],
      "depends_on": ["04", "05", "08"],
      "is_gate": false,
      "gateType": "auto"
    }
  ]
}
