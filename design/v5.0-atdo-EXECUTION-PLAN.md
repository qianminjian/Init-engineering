# v5.0 atdo 执行计划 — Init Engineering 架构修复

> 基于 Phase 1 架构审计输出，Phase 2 待办清单生成
> 创建：2026-07-01

## Phase 01: P0-2 实现 — default_match_exclude

**目标**: 实现 `_shared/exclude.py` 中的 `parse_exclude_callback` 和 `default_match_exclude`

**任务**:
- 创建 `auto_engineering/init/_shared/exclude.py`
- 实现 `default_match_exclude(Path) -> bool`：排除 .git/ .venv/ __pycache__/ 等
- 实现 `parse_exclude_callback(spec: str) -> Callable[[Path], bool]`
- 写单测覆盖：合法目录不排除、非法目录排除

**验收标准**:
- `pytest tests/test_exclude.py -v --no-cov --timeout=60` 通过
- `_proc-use/` 外目录不被默认排除规则误排除

---

## Phase 02: P1-1 实现 — templates_suffix + preserve_symlinks CLI 透传

**目标**: InitWorker/CLI 透传 `templates_suffix` 和 `preserve_symlinks` 到 TemplateRenderer

**任务**:
- InitWorker.__init__ 添加 `templates_suffix: str = ".jinja"` 参数
- InitWorker.__init__ 添加 `preserve_symlinks: bool = True` 参数
- InitWorker._phase_render() 将这两个参数透传给 `render_to()`
- scaffold_render.render_to() 签名确认接收这两个参数
- CLI `--help` 显示新参数

**验收标准**:
- `ae init --help` 显示 `--templates-suffix` 和 `--preserve-symlinks`
- `ae init --templates-suffix .tmpl` 正常工作

**依赖**: Phase 01

---

## Phase 03: P1-2 实现 — 5 类钩子

**目标**: 实现设计 §5.2 的钩子执行框架

**任务**:
- 实现 `before_renderer_hook`
- 实现 `after_renderer_hook`
- 实现 `before_copy_file_hook`
- 实现 `after_copy_file_hook`
- 实现 `on_exists_hook`
- 在 scaffold_phases.py 的 `_phase_render()` 中调用钩子

**验收标准**:
- 钩子按序执行，失败正确处理（log + 继续或中断可配置）

**依赖**: Phase 01

---

## Phase 04: P1-3 实现 — ProjectEnvironment sandbox_roots 集成

**目标**: `ProjectEnvironment` 集成 `sandbox_roots` 防止 `!include` 路径穿越

**任务**:
- 读取 `config/environment.py` 的 `ProjectEnvironment` 类
- 在 `!include` 解析路径验证时注入 `sandbox_roots` 检查
- 路径穿越被拒绝时抛出安全异常

**验收标准**:
- `!include ../../etc/passwd` 被拒绝
- 合法 `!include` 路径正常工作

**依赖**: Phase 02

---

## Phase 05: P1-4 + P1-5 实现 — TemplateConfig preserve_symlinks + templates_suffix 透传

**目标**: YAML 配置文件的 `preserve_symlinks` 和 `templates_suffix` 透传到 TemplateRenderer

**任务**:
- 读取 `_shared/config_types.py` 的 `TemplateConfig`
- 确认 `preserve_symlinks: bool = True` 和 `templates_suffix: str = ".jinja"` 字段存在
- 在 `config_loader.py` 加载配置后透传给 TemplateRenderer

**验收标准**:
- YAML 配置可控制 symlink 行为和模板后缀

**依赖**: Phase 02

---

## Phase 06: P2-3 实现 — !include 路径安全验证

**目标**: YAML `!include` 路径安全验证，防止目录穿越

**任务**:
- 在 `config_loader.py` 的 `!include` 处理逻辑中添加 `sandbox_roots` 验证
- 使用 `os.path.realpath` 双侧归一化（参考 renderer.py path traversal guard）
- 非法路径抛出 `ConfigLoaderSecurityError`

**验收标准**:
- `!include /etc/passwd` 被拒绝
- `!include ../../root/.ssh/id_rsa` 被拒绝

**依赖**: Phase 04

---

## Phase 07: 全量测试 + 覆盖率 ≥ 80%

**目标**: 397 测试全绿，覆盖率达标

**任务**:
- 运行 `pytest tests/ --cov=auto_engineering --cov-report=term-missing --timeout=120`
- 覆盖率未达标时补充缺失用例
- 全部 P0/P1/P2 问题修复验证

**验收标准**:
- 所有测试 PASS
- 覆盖率 ≥ 80%

**依赖**: Phase 06

---

## 优先级排序

| Phase | 任务 | 优先级 |
|-------|------|--------|
| 01 | P0-2: default_match_exclude | P0 |
| 02 | P1-1: templates_suffix + preserve_symlinks 透传 | P1 |
| 03 | P1-2: 5 类钩子 | P1 |
| 04 | P1-3: ProjectEnvironment sandbox_roots | P1 |
| 05 | P1-4 + P1-5: TemplateConfig 透传 | P1 |
| 06 | P2-3: !include 路径安全验证 | P2 |
| 07 | 全量测试 + 覆盖率验证 | P0 |
