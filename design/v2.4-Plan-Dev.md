{
  "planName": "Auto-Engineering v2.4 — 全面架构修复（12 项优化）",
  "planDescription": "v2.4 全面架构修复: P0 阻断 (3 项) + P1 一致性 (5 项) + P2 长期 (4 项)。来源: /tttt 第三轮架构审计（2026-06-26）。Phase 3 提示词要求'整合到开发计划表 + 可以被atdo执行'。",
  "auditSource": "design/v2.3-Plan-Dev.md 同级审计 (本轮 /tttt 输出) — 缺失参考项目对比（LangGraph/AutoGen/CrewAI 源码），仅基于本项目代码审计",
  "phases": [
    {
      "id": "01",
      "name": "P0-A: 明确 v2.0 Channel 体系归属（BEACON 决策 23）",
      "goal": "明确 v2.0 Channel 体系是 (a) 真用 还是 (b) checkpoint 专用。选一个不动，消除 type 双义。",
      "success_criteria": [
        "BEACON.md 增加决策 23，明确选择 (a) 或 (b) 及理由",
        "如果选 (a)：orchestrator.py / runtime.py / gates/ 全部改用 loop.state.LoopState；engine.state.LoopState 标记 deprecated",
        "如果选 (b)：删除 loop.state.LoopState；Channel 类保留供 checkpoint/migrate.py 使用",
        "全量测试 870 PASS，IDE 不再报告同名两义",
        "grep 'from auto_engineering.engine.state import LoopState' 在 v2.0 path 中为 0 处"
      ],
      "tasks": [
        "设计决策: 选 (a) 真用 v2.0 Channel-based LoopState, 或 (b) 让 Channel 仅供 checkpoint 使用",
        "按选择更新 BEACON.md 增加决策 23 记录（含理由 + 备选方案 + 取舍）",
        "如果选 (a): 更新 orchestrator.py / runtime.py / gates/ 全部用 loop.state.LoopState",
        "如果选 (b): 删除 loop.state.LoopState，保留 Channel 类（仅供 checkpoint/migrate.py 用）",
        "更新所有 import 引用，更新 type hints",
        "跑全量测试，确认 870 PASS + 无 type 双义"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "02",
      "name": "P0-B: ae checkpoint 命令切到 v2.0 SQLiteCheckpointStore",
      "goal": "cli.py 不再 import engine.checkpoint；v1.0 engine.checkpoint 冻结（决策 24）；4 个 checkpoint 子命令用 loop.checkpoint",
      "success_criteria": [
        "cli.py line 1071/1107/1149 改为 from auto_engineering.loop.checkpoint import SQLiteCheckpointStore",
        "BEACON.md 增加决策 24：engine/checkpoint.py 冻结",
        "engine/checkpoint.py 文件头添加 ⚠️ 冻结标记（参考 builtin.py 决策 22 风格）",
        "4 个 ae checkpoint 子命令（list/show/delete/resume）改用 v2.0 SQLiteCheckpointStore",
        "test_checkpoint_cli.py 全 PASS（不破坏）",
        "grep 'from auto_engineering.engine.checkpoint import' 在 cli.py 中为 0 处"
      ],
      "tasks": [
        "cli.py 三个 import 改为 loop.checkpoint.SQLiteCheckpointStore",
        "BEACON.md 增加决策 24：engine/checkpoint.py 冻结",
        "engine/checkpoint.py 文件头加 ⚠️ 冻结标记 + 引导用 loop.checkpoint",
        "更新 4 个 checkpoint 子命令调用（list/show/delete/resume）",
        "跑 test_checkpoint_cli.py 验证不破坏",
        "跑全量测试确认 870 PASS"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "03",
      "name": "P0-C: Coverage Gate 真工作 或 冻结",
      "goal": "Coverage Gate 实际产生覆盖率数据，或文件头加 ⚠️ 冻结标记 + 引导 CI 单独负责",
      "success_criteria": [
        "决策 a 或 b 并 BEACON 记录（决策 25）",
        "如果选 (a)：pytest-cov 安装 + pyproject.toml addopts 含 --cov=auto_engineering + Gate 跑出真实数据 + coverage 报告存在",
        "如果选 (b)：gates/coverage.py 文件头加 ⚠️ 冻结标记，运行时 print DeprecationWarning 1 次",
        "无论选哪个：全量测试 PASS"
      ],
      "tasks": [
        "决策 (a) 装 pytest-cov + 配 pyproject.toml，或 (b) 冻结 CoverageGate",
        "如果选 (a): pip install pytest-cov, 改 pyproject.toml addopts, 验证 Gate 跑出真实数据",
        "如果选 (b): gates/coverage.py 文件头加 ⚠️ 冻结, 加 DeprecationWarning, BEACON 决策 25",
        "跑全量测试确认不破坏"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "04",
      "name": "P1-C: gates/builtin.py 加运行时 DeprecationWarning",
      "goal": "builtin.py 运行时 print warning（不阻塞）；BEACON 决策 26 记录；引导用户用 v2.0 Gate",
      "success_criteria": [
        "gates/builtin.py 文件头加 ⚠️ 冻结 + DeprecationWarning 描述（已有决策 22，加运行时 warning 升级到决策 26）",
        "运行时 RequirementGuardrail / 4 个其他 Guardrail 触发时 print DeprecationWarning（仅 1 次）",
        "BEACON.md 增加决策 26",
        "test_builtin.py 全 PASS + 加 deprecation 触发测试",
        "Warning 不阻塞 dev_loop 主流程"
      ],
      "tasks": [
        "gates/builtin.py 运行时加 DeprecationWarning（用 warnings.warn, once=True）",
        "BEACON.md 增加决策 26",
        "test_builtin.py 加 deprecation 触发测试（验证 warning 被触发 1 次）",
        "跑全量测试"
      ],
      "depends_on": ["02"],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "05",
      "name": "P1-D: state.py 拆 3 文件（channels/loop_state/metrics）",
      "goal": "loop/state.py 687 行 → 拆为 state/channels.py + state/loop_state.py + state/metrics.py；旧 import 路径保持向后兼容（re-export）",
      "success_criteria": [
        "state/channels.py 含 Channel ABC + LastValueChannel + AccumulatingChannel + BarrierChannel",
        "state/loop_state.py 含 LoopState Pydantic 模型 + 序列化三件套（copy / checkpoint / from_checkpoint）",
        "state/metrics.py 含 MetricsSnapshot + Signal",
        "state/__init__.py re-export 所有符号（保持 import 路径兼容）",
        "原 state.py 删除或转为 import 转发",
        "全量测试 870 PASS，0 破坏"
      ],
      "tasks": [
        "新建 state/channels.py（Channel ABC + 3 个具体 Channel 类）",
        "新建 state/loop_state.py（LoopState Pydantic + 序列化）",
        "新建 state/metrics.py（MetricsSnapshot + Signal）",
        "state/__init__.py re-export 所有符号",
        "删除或转发原 state.py",
        "跑全量测试验证向后兼容"
      ],
      "depends_on": ["01"],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "06",
      "name": "P1-A: 3 Agent 类合并为 1 个 Agent(role, system_prompt, llm)",
      "goal": "删除 architect/developer/critic.py；BaseAgent 接受 role+system_prompt 参数；测试 3 Agent 的所有功能 PASS",
      "success_criteria": [
        "architect.py / developer.py / critic.py 删（或转为 module-level const 形式）",
        "BaseAgent 接受 role: str 字段 + system_prompt 参数",
        "agents/__init__.py 导出 Agent + 3 个 system_prompt 常量",
        "cli.py import 改为统一 Agent + 3 个 system_prompt",
        "BEACON 决策 27：Agent 类合并",
        "全量测试 870 PASS，所有 ArchitectAgent/DeveloperAgent/CriticAgent 引用更新"
      ],
      "tasks": [
        "agents/architect.py / developer.py / critic.py 转 module-level const（ARCHITECT_SYSTEM_PROMPT 等）",
        "agents/base.py: BaseAgent 接受 role: str 字段",
        "agents/__init__.py 导出 Agent + 3 个 prompt 常量",
        "更新 cli.py 所有 Agent 引用（import + 实例化）",
        "BEACON 决策 27",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "07",
      "name": "P1-B: 解决类型双义（engine.state.LoopState → EngineState）",
      "goal": "engine.state.LoopState → EngineState；runtime.task.Task → AgentTask；v2.0 保留 LoopState 和 Task",
      "success_criteria": [
        "engine.state.LoopState 改名为 EngineState（type alias LoopState = EngineState 保持向后兼容）",
        "runtime.task.Task 改名为 AgentTask（type alias Task = AgentTask 保持向后兼容）",
        "所有 import 引用更新（cli.py / runtime/ / gates/builtin.py / gates/guardrail.py / loop/orchestrator.py 等）",
        "BEACON 决策 28：类型双义解决",
        "全量测试 870 PASS，IDE 不再报告同名两义"
      ],
      "tasks": [
        "engine/state.py: LoopState 改名为 EngineState，加 type alias 保持兼容",
        "runtime/task.py: Task 改名为 AgentTask，加 type alias 保持兼容",
        "更新所有 import 引用（grep + replace）",
        "BEACON 决策 28",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "08",
      "name": "P1-E: ClaudeSemanticEvaluator 复用 AnthropicProvider",
      "goal": "ClaudeSemanticEvaluator.__init__ 中预创建 provider，多次 __call__ 复用；避免每轮 SDK client 重连开销",
      "success_criteria": [
        "ClaudeSemanticEvaluator.__post_init__ 创建 self._provider（而非每轮 _call_claude 创建）",
        "_call_claude 改为 self._provider.create_message",
        "测试验证同一 provider 引用（id 一致）在多次 __call__ 后保持",
        "test_loop_semantic_evaluator.py 9 用例 PASS"
      ],
      "tasks": [
        "semantic_evaluator.py: __post_init__ 创建 self._provider",
        "_call_claude 改用 self._provider",
        "加测试验证 provider 复用（多次 __call__ 后 id 相同）",
        "跑 test_loop_semantic_evaluator.py"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "09",
      "name": "P2-A: ae init 系统 13+ 文件减至 8 文件 + 分层",
      "goal": "init/ 13+ 文件减至 8 文件，合并 prompts + answers + detector（都属'用户交互'层）",
      "success_criteria": [
        "prompts.py + answers.py + detector.py → interaction.py（用户交互层）",
        "scaffold_phases.py + scaffold_render.py + scaffold_hooks.py → scaffold_pipeline.py（执行流水线层）",
        "config_loader.py + config_types.py + config.py → config.py（配置层）",
        "renderer.py / scaffold.py / errors.py / hooks.py 保留",
        "templates/ 目录不动",
        "test_init_e2e_scaffold.py 8 个项目类型 E2E 全过"
      ],
      "tasks": [
        "合并 prompts.py + answers.py + detector.py → interaction.py",
        "合并 scaffold_phases.py + scaffold_render.py + scaffold_hooks.py → scaffold_pipeline.py",
        "合并 config_loader.py + config_types.py + config.py → config.py",
        "更新 import 引用（grep + replace）",
        "BEACON 决策 29",
        "跑 test_init_e2e_scaffold.py 验证 8 项目类型 E2E"
      ],
      "depends_on": [],
      "is_gate": true,
      "gateType": "auto"
    },
    {
      "id": "10",
      "name": "P2-B: errors.py ErrorCode 清理（20+ → 12 + 注释）",
      "goal": "删除未抛出的枚举值，每个加'何时抛出'注释",
      "success_criteria": [
        "grep 统计 ErrorCode.* 实际抛出处数 ≤ 12",
        "删除未抛出的枚举值",
        "每个保留枚举值加注释 '# 在 X 路径抛出, 由 Y 触发'",
        "全量测试 PASS，错误码未被破坏"
      ],
      "tasks": [
        "grep 统计实际抛出的 ErrorCode 枚举值",
        "删除未抛出的枚举值（保守: ≤ 12）",
        "每个保留枚举值加注释",
        "BEACON 决策 30",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "11",
      "name": "P2-C: _features/ae-feature.yml 字段定义",
      "goal": "在 init/templates 加字段说明；或明确'不实现'并删引用",
      "success_criteria": [
        "grep 'ae-feature' 找到所有引用点",
        "若 R17 需要：在 init/templates/_features/ae-feature.yml 加 schema 说明",
        "若不实现：删 BEACON §待解决问题 引用",
        "test_init_design_docs.py 反映决定并 PASS"
      ],
      "tasks": [
        "grep 'ae-feature' 找引用点",
        "决策: 实现还是删除",
        "按决策更新",
        "更新 test_init_design_docs.py",
        "跑测试验证"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    },
    {
      "id": "12",
      "name": "P2-D: Plan validate 加 workspace 边界检查",
      "goal": "target_files 必须在 project_root 下，否则抛 ConflictError",
      "success_criteria": [
        "loop/plan.py: check_file_isolation 加 workspace 边界校验",
        "若 target_files 含绝对路径或 '../' 路径，抛 ConflictError",
        "加测试 test_workspace_boundary_validation",
        "全量 870 PASS + 新测试 PASS"
      ],
      "tasks": [
        "loop/plan.py: check_file_isolation 加 workspace 边界校验",
        "加 ConflictError 抛出逻辑",
        "加测试 test_workspace_boundary_validation",
        "跑全量测试"
      ],
      "depends_on": [],
      "is_gate": false,
      "gateType": "auto"
    }
  ]
}
