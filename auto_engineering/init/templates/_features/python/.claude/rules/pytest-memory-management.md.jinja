---
name: pytest-memory-management
description: pytest 内存管理规范 — 防止 pytest + coverage + fixture 在受限环境中撑爆内存
type: project
---

# pytest 内存管理规则

> 创建：2026-06-25 | 来源：auto-engineering 项目实战沉淀（已通过 R26 集成至 init 模板）
> 约束：CI 容器 / 低内存机器 / 受限开发环境 — pytest 子进程 + coverage + fixture 容易撑爆内存

## 核心约束

- pytest + coverage instrumentation 内存叠加约 **2x**
- conftest hook 写入的 `.ae-cache/*_test_failures.json` 类文件跨 session 累积
- `.pytest_cache` 跨项目累积

## 禁止的反模式

1. 全量跑 `pytest tests/` — 加载所有 fixture，~30-60s 且内存叠加
2. 后台跑 pytest — 会 hang 无人收尸
3. 并发跑多个 pytest 进程 — 内存叠加 ~200MB/进程
4. 跑完不清理 — 临时文件 + `.pytest_cache` 累积
5. 不带 `--timeout` 的长跑 — hang 时无法终止

## 推荐调用方式

### 单文件测试（最高频）
```bash
.venv/bin/pytest tests/test_xxx.py -v --no-cov --timeout=60
```

### 关键字过滤
```bash
.venv/bin/pytest -k "test_execute" --no-cov --timeout=60
```

### 全量验证（开发节点，少用）
```bash
.venv/bin/pytest tests/ --no-cov --timeout=120 -q
```

### 跑覆盖率（独立 opt-in，需显式指定）
```bash
.venv/bin/pytest tests/ --cov=<package_name> --cov-report=term-missing --timeout=120
```

## 清理协议（每次 pytest 后必做）

```bash
rm -f .ae-cache/*_test_failures.json   # conftest hook 累积
rm -rf .pytest_cache                   # 测试 cache（含 tests/.pytest_cache）
```

或统一：`make clean`（如存在 Makefile）

## 推荐 pyproject.toml 配置

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--timeout=60", "-p no:cacheprovider"]  # cov 默认关闭
```

- `--timeout=60`：单测试最多 60s，超时强制 fail（防 hang）
- `-p no:cacheprovider`：禁用 pytest cache（省内存 + 避免脏读）
- **cov 默认关闭**：跑覆盖率需显式 `--cov=<package_name>`

## 紧急处理（已爆内存时）

```bash
# 1. 找 pytest 进程
ps aux | grep -E "pytest|python" | grep -v grep

# 2. 强杀具体 PID（不要 kill -9 全 python）
kill -9 <PID>

# 3. 清残留
rm -f .ae-cache/*_test_failures.json
rm -rf .pytest_cache tests/.pytest_cache

# 4. 验证释放
free -h
```

## 不适用时

如果项目运行在充足资源环境（CI 容器、高内存开发机），且确认不需要此规则：

- 直接删除本文件
- 同时删除 `CLAUDE.md` 中对本文件的 @ 引用
- 调整 `pyproject.toml` 的 `addopts` 恢复为自定义配置

## Why

源自 auto-engineering 项目自身实战：pytest + 默认 `--cov` 在受限环境中频繁撑爆内存。本规则作为产品级最佳实践沉淀，所有 `ae init` 出来的项目均自动获得。如不适用可按上文删除。